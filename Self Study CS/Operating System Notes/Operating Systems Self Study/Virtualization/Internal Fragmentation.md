When an allocator allocates more memory than what is asked. This creates wasted memory. But some of this needed for memory metadata (like knowing how much memory is allocated for a particular thing)

See [[External Fragmentation]] as well.

